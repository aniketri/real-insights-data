{
  "buildCommand": "cd .. && pnpm install --frozen-lockfile && pnpm db:generate && pnpm build:web",
  "installCommand": "cd .. && npm install -g pnpm@9.6.0 && pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "apps/web/src/app/api/**": {
      "maxDuration": 30
    }
  },

  "build": {
    "env": {
      "NEXT_PUBLIC_API_URL": "https://your-api.onrender.com"
    }
  }
} 