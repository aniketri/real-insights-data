{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile --no-optional && cd packages/db && pnpm exec prisma generate --schema=schema.prisma && cd ../../apps/web && pnpm build",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile --no-optional",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "src/app/api/**": {
      "maxDuration": 30
    }
  },
  "build": {
    "env": {
      "NODE_ENV": "production",
      "SKIP_ENV_VALIDATION": "true"
    }
  }
} 