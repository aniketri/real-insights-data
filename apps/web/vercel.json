{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm install --filter web... && cd packages/db && npx prisma generate --schema=schema.prisma && cd ../../apps/web && pnpm run build",
  "installCommand": "cd ../.. && pnpm install --filter web...",
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "functions": {
    "src/app/api/**": {
      "maxDuration": 30
    }
  },
  "build": {
    "env": {
      "NODE_ENV": "production",
      "SKIP_ENV_VALIDATION": "true"
    }
  }
} 